name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt update
          sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libssl-dev cmake

      - name: Install Python Build Tools
        run: |
          pip install --upgrade pip
          pip install buildozer cython kivy pillow

      - name: Build APK with Buildozer
        run: |
          cd $GITHUB_WORKSPACE
          
          # Force create the android configuration paths
          mkdir -p ~/.android
          touch ~/.android/repositories.cfg
          
          # Force accept all android SDK licenses before running buildozer
          # This specifically clears the 'Skipping following packages as the license is not accepted' error.
          mkdir -p $HOME/.buildozer/android/platform/android-sdk/licenses || true
          yes | sdkmanager --licenses || true

          # Run buildozer with standard compilation configurations
          buildozer --log_level=2 android debug
        env:
          APP_ANDROID_ARCHS: armeabi-v7a
          
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: compiled-apk
          path: bin/*.apk
